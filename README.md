# Command(perintah)

$ apt update && apt upgrade

$ apt install python

$ apt install figlet

$ apt install git

$ git clone https://github.com/Zaeni123/Script_login

$ cd Script_login

$ python2 login.py
